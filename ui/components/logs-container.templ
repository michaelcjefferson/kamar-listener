package components

import (
  "github.com/michaelcjefferson/kamar-listener/internal/data"
)

templ LogsContainer(logs []*data.Log, metadata data.Metadata) {
  <div id="logs-container">
    @LogMetadata(metadata)

    @LogList(logs)

    // @PageNavigationControls("logs", metadata)
  </div>
}